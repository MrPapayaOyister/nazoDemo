import type { TemplateCategory } from '@/types'

/** Arabic labels for template categories (English is the enum value itself). */
export const CATEGORY_AR: Record<TemplateCategory, string> = {
  Approval: 'اعتماد',
  Circular: 'تعميم',
  Announcement: 'إعلان',
}
